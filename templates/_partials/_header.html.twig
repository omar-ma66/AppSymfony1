
<header style="background-color: #2c3e50; color: white; padding: 15px 30px; display: flex; justify-content: space-between; align-items: center;">
    <h1 style="margin: 0; font-size: 1.4em;">
        <a href="{{ path('app_home') }}" style="color: white; text-decoration: none;">📚 Bibliotech</a>
    </h1>
    <nav>
         <a href="{{ path('app_auteur_index') }}" style="color: white; margin-left: 20px; text-decoration: none; {% if app.current_route == 'app_auteur_index' %}font-weight: bold; text-decoration: underline;{% endif %}">Auteurs</a>
        <a href="{{ path('app_home') }}" style="color: white; margin-left: 20px; text-decoration: none; {% if app.current_route == 'app_home' %}font-weight: bold; text-decoration: underline;{% endif %}">
            Accueil
        </a>
        <a href="{{ path('app_liste_livre') }}" style="color: white; margin-left: 20px; text-decoration: none; {% if app.current_route == 'app_book_index' %}font-weight: bold; text-decoration: underline;{% endif %}">
            Livres
        </a>
        <a href="{{ path('app_about') }}" style="color: white; margin-left: 20px; text-decoration: none; {% if app.current_route == 'app_about' %}font-weight: bold; text-decoration: underline;{% endif %}">
            À propos
        </a>
    </nav>
</header>